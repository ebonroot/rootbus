syntax = "proto3";

package Rootbus.Protobuf;

import "msg/key_value.proto";

message Record {
  enum SchemaType {
    player = 0;
  }
  // required
  SchemaType schema = 1;
  map <string, KeyValue> data = 4;
}
